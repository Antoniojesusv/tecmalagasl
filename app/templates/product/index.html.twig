{% extends 'AdminPanel/index.html.twig' %}

{% block title %}Product index
{% endblock %}

{% block body %}{% endblock %}

{% block main %}
	<section class="section-new row justify-content-end">
		<a href="{{ path('app_product_add') }}">
			<button class="btn btn-success">
				<i class="fas fa-plus"></i>
				Nuevo</button>
		</a>
	</section>
	{{ include('/product/table-products.html.twig') }}
	<div class="pagerfanta">
		{{ pagerfanta(my_pager, 'twitter_bootstrap4') }}
	</div>
{% endblock %}

<form class="deleteForm" method="post" action="{{ path('app_characteristic_delete', {'id': characteristic.id}) }}">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ characteristic.id) }}">
	<button class="btn btn-danger">Eliminar</button>
</form>
